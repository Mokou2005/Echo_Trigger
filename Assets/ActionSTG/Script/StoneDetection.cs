

/*public class StoneDetection : MonoBehaviour
{
    public Transform stone; //親(Stone)をセットしておく

    private void OnTriggerEnter(Collider other)
    {
        if (other.CompareTag("Enemy"))
        {
        
            EnemyAI ai = other.GetComponent<EnemyAI>();
            if (ai != null)
            {
                ai.OnDetectStone(stone.position); //敵に石の位置を渡す
            }
        }
    }
}*/

